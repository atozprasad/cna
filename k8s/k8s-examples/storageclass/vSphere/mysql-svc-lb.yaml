apiVersion: v1
kind: Service
metadata:
  name: mysql
  labels:
    app: mysql
  #annotations:
   #service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
 #externalTrafficPolicy: Local
  ports:
  - port: 3306
  selector:
    app: mysql
  clusterIP: None
  type: ClusterIP
#LoadBalancer


#apiVersion: v1
#kind: Service
#metadata:
  #name: mysql
#spec:
  #ports:
  #- port: 3306
  #selector:
    #app: mysql
  #clusterIP: None
